import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    height: 300,
    width: "100%",
    borderWidth: 2,
    borderColor: "#1976d2",
    borderRadius: 16,
    marginBottom: 16,
    backgroundColor: "#fff",
    overflow: "hidden",
  },
});

export default styles;
